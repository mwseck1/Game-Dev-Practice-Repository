using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CorporationController : Player
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(playerId);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
